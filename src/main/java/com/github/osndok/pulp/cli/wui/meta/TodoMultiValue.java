package com.github.osndok.pulp.cli.wui.meta;

/**
 * Used to mark fields known to accept multiple values, in case we ever create a solution therefor.
 * TODO: BUG: many arguments are meant to be repeatable, but we only accept one value.
 */
public
@interface TodoMultiValue
{
}
